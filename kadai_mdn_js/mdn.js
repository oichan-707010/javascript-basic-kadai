const currently = new Date();
const nowYear = currently.getFullYear();
const nowMonth = currently.getMonth();
const nowDate = currently.getDate();
console.log(nowYear + "年" + nowMonth + "月" + nowDate + "日");
